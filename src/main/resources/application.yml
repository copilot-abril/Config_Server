eureka:
  instance:
    instance-id: "${spring.application.name}:${random.value}"
  client:
    service-url:
      defaultZone: https://eureka-production-cd7c.up.railway.app/
server:
  port: 7777
spring:
  application:
    name: config-server
  profiles:
    active: default
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MixelMeza/config-server.git
          default-label: master
          clone-on-start: true
          username: MixelMeza
          password: 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

